# Pi Context 20260910.2138.3

新增上下文占用、压缩状态、前后估算规模、原文保留与摘要覆盖指标；由宿主补充真实压缩终态与推理开销。兼容升级保留配置和可用摘要，不为填充观测重新压缩。需要先安装支持 ABI 3 的 App，再安装并启用本包。真实模型与原生界面待用户验收。

先安装 [App 20260910.2135-3](https://github.com/ykswang/LLMHubX/releases/tag/app-v20260910.2135-3)，再在插件市场更新 Pi Context，或导入本目录中的 `.lhxp`，确认启用新版本。原有管家绑定、配置和可用摘要在兼容升级时保留。只更新 App 不会自动替换已安装插件。

观测页每个插件显示一张业务卡片；选择管家与上下文查看具体占用、压缩效果和事件。尚未触发压缩时显示真实构建状态，不补算历史压缩，不用零值代替未采集数据。运行诊断默认折叠。

旧 App 不支持本包格式或 ABI，需先升级 App；旧版市场可能提示版本格式不支持，手动导入可能提示不支持 observation 目录。

基础 Hook/Resource 插件仍沿用已有运行诊断，本次不将其全部升级为业务指标插件。
