# CMW Rust SDK 20260831.1009-2

- 新增 `tool-component` / `tool-runtime` WIT world 与 `ToolDispatch` 强类型 Rust 绑定。
- 保留 Workflow 与 Expert contribution 的 ABI Level 2 绑定，并内含本次 Level 1 SDK 快照。
- SDK 移入独立的 Plugin Platform 工程目录，并新增可验证的 `sdk.toml` build、test 与 package 描述。
